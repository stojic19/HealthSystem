import { RenovationTermsRequest } from './renovation-terms-request.model';

describe('RenovationTermsRequest', () => {
  it('should create an instance', () => {
    expect(new RenovationTermsRequest()).toBeTruthy();
  });
});
